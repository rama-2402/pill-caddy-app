<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.voidd.pillcaddy">

    <uses-permission android:name="android.permission.INTERNET" />
    
    <application
        android:name=".KodeinApplication"
        android:allowBackup="true"
        android:icon="@mipmap/pill_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pill_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pillcaddy">
        <activity android:name=".auth.SignUpActivity">

        </activity>
        <activity android:name=".auth.SignInActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homepage.HomeActivity" >

        </activity>
    </application>

</manifest>